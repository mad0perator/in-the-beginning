# environment.yml -- Local Mamba Environment Definition
channels:
  - conda-forge
dependencies:
  - jupyterlab
  - ipywidgets
  - numpy
  - pandas
  - rich
  - pip
  - pip:
    - jupyterlab-quarto
prefix: "./envs"
